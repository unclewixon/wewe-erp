{
  "name": "wewe-erp",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "pnpm": {
    "overrides": {
      "js-yaml": ">=4.3.0",
      "vite": ">=6.4.3",
      "vitest": ">=3.2.6",
      "esbuild": ">=0.25.0"
    }
  }
}